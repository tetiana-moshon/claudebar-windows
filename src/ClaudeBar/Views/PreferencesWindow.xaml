<Window x:Class="ClaudeBar.Views.PreferencesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ClaudeBar Settings"
        Width="440" Height="400"
        MinWidth="400" MinHeight="360"
        Background="#FF1E1E20"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="24,18,24,18">
            <TextBlock Text="Settings" FontSize="22" FontWeight="Bold" Foreground="#FFF2F2F7"/>
            <TextBlock Text="Preferences apply immediately." FontSize="11" Foreground="#FF9A9AA0"/>
        </StackPanel>

        <Border Grid.Row="1" Height="1" Background="#1AFFFFFF"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel x:Name="ContentHost" Margin="24,18,24,18"/>
        </ScrollViewer>
    </Grid>
</Window>
